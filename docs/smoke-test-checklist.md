<!-- Ported from: creation-space (2026-03-24) -->
# フロントエンド Smoke Test チェックリスト (#139)

UI 変更を含む Issue の close 前に確認する。

## 手動チェック項目

### 1. Three.js シーン描画
- [ ] キャンバスが表示され、Three.js シーンが描画されている
- [ ] カメラ操作（回転・ズーム）が機能する
- [ ] パーティクル/メッシュが正常にレンダリングされている
- [ ] FPS が安定している（著しいフレームドロップがない）

### 2. ナビバー
- [ ] トップバーが表示され、タイトルが正しい
- [ ] ハンバーガーメニューが開閉する
- [ ] メニュー外クリックでハンバーガーメニューが閉じる
- [ ] ナビゲーションリンクが正しいセクションに遷移する
- [ ] レスポンシブ: xl (1200px) ブレークポイントで正しく切り替わる

### 3. devlog セクション
- [ ] devlog カードが表示される
- [ ] カード内のタイトル・日付・概要が表示される
- [ ] カードクリックで詳細が表示される

### 4. ビューワー
- [ ] MD ビューワーが開く
- [ ] Markdown 本文がレンダリングされる
- [ ] 閉じるボタンが機能する

### 5. コンソールエラー
- [ ] ブラウザの DevTools コンソールにエラーがない
- [ ] ネットワークタブで 404 がない

### 6. FPS モニター（?dev モード）
- [ ] `?dev` パラメータ付きでアクセスし、FPS カウンターが表示される
- [ ] FPS が 30 以上を維持している

## 自動テスト

```bash
node tests/config-consistency.test.js
```

config.js の CSS 変数参照との整合性を自動検証する（62 assertions）。

## チェックリスト使用時の注意

- ローカルサーバーで確認: `./serve.sh` -> http://localhost:3001/
- モバイル幅（375px）とデスクトップ幅（1440px）の両方で確認する
- GitHub Pages デプロイ後にも本番URLで再確認する
