# DISABLED: sessions.json の自動上書きが本番データを破壊するため無効化
# 手動実行のみ許可（workflow_dispatch）
# 将来的に安全な形で再設計する場合はこのファイルを編集

name: Devlog Gallery — Update sessions.json (DISABLED)

on:
  workflow_dispatch:  # 手動実行のみ

permissions:
  contents: write

jobs:
  update-sessions:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "⚠️ このワークフローは現在無効化されています"
          echo "sessions.json は手動で管理してください"
          exit 1
